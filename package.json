{
  "name": "photopea-alpha-split",
  "version": "1.0.1",
  "private": true,
  "description": "Photopea plugin that splits a layer into separate layers by detecting disconnected alpha regions.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "license": "MIT"
}
