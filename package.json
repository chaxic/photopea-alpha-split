{
  "name": "photopea-alpha-split",
  "version": "1.1.0",
  "private": true,
  "description": "Photopea plugin that detects disconnected alpha regions and exports each as a cropped PNG.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "license": "MIT"
}
